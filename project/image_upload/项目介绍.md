>  一个图片上传至云服务商的 saas 功能，支持 apikey，sdk 方式集成到不同前端项目

## 需求分析

## 技术选型
- Nextjs（tailwind css + ts）
- Database —— orm drizzle + pg 
- UI —— shadcn/ui



node --input-type=module -e "import postgres from 'postgres'; const sql = postgres('postgres://postgres: fxl20031020@localhost:5432/postgres'); const rows = await sql.unsafe('select 1 as ok'); console.log(rows); await sql.end();"
